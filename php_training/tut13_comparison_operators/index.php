<?php

    //comparison operator
    /*
        > Greater than
        < Less than
        >= Greater than or equal to
        <= Less than or equal to
        == equal to
        != not equal to
        === identical (same value and same data type. ex. 1 as a integer is not identical to 1.0 which is a float)

    */

    $apples = 1;
    $oranges = 1.0;
    if ($apples === $oranges)
    {
        echo 'apples and oranges are equal';
    }

?>
